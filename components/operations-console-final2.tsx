'use client'
import { useEffect, useState, type ReactNode } from 'react'
import { createClient } from '@/lib/supabase/client'
import DataTable from '@/components/data-table'
type Row=Record<string,any>
type Module='inventory'|'production'|'orders'|'sales'|'payments'
const sb=()=>createClient(); const today=()=>new Date().toISOString().slice(0,10); const txt=(v:any)=>String(v??'')
function Field({label,children}:{label:string;children:ReactNode}){return <label className="field"><span>{label}</span>{children}</label>}
function Select({value,onChange,children}:{value:string;onChange:(v:string)=>void;children:ReactNode}){return <select value={value} onChange={e=>onChange(e.target.value)}>{children}</select>}
function Status({message,error=false}:{message:string;error?:boolean}){return message?<p className={`form-status${error?' form-status-error':''}`} role="alert">{message}</p>:null}
function Modal({title,close,children,wide=false}:{title:string;close:()=>void;children:ReactNode;wide?:boolean}){return <div className="modal-backdrop"><div className={`modal-card${wide?' purchase-modal':''}`}><div className="modal-header"><h3>{title}</h3><button className="secondary-button" onClick={close}>Close</button></div>{children}</div></div>}
function useRows(table:string){const[r,setR]=useState<Row[]>([]);const load=async()=>{const{data}=await sb().from(table).select('*').limit(500);setR(data??[])};useEffect(()=>{void load()},[table]);return[r,load] as const}
function Page({title,description,action,children}:{title:string;description:string;action:string;children:ReactNode}){return <section className="page-panel"><div className="section-label">Mane Masala</div><div className="master-header"><div><h1>{title}</h1><p className="page-intro">{description}</p></div>{children}</div></section>}
function Orders(){
 const[customers]=useRows('customers'),[agents]=useRows('sub_agents'),[items]=useRows('items'),[orders,loadOrders]=useRows('v_orders_list');
 const[open,setOpen]=useState(false),[drafts,setDrafts]=useState<any[]>([blank()]),[planId,setPlanId]=useState(''),[planLines,setPlanLines]=useState<any[]>([]),[recipes,setRecipes]=useState<any[]>([]),[planMsg,setPlanMsg]=useState(''),[planError,setPlanError]=useState(false),[saving,setSaving]=useState(false),[selectedOrder,setSelectedOrder]=useState<Row|null>(null);
 function blank(){return{party:'direct',customerId:'',subAgentId:'',endMode:'existing',endCustomerId:'',endName:'',endPhone:'',source:'whatsapp',date:today(),dispatchDate:'',advance:'',requests:'',notes:'',lines:[{itemId:'',quantity:'',rate:''}]}}
 const patch=(i:number,k:string,v:any)=>setDrafts(x=>x.map((d,n)=>n===i?{...d,[k]:v}:d));
 const line=(oi:number,li:number,k:string,v:any)=>setDrafts(x=>x.map((d,n)=>n===oi?{...d,lines:d.lines.map((l:any,j:number)=>j===li?{...l,[k]:v}:l)}:d));
 const save=async()=>{for(let i=0;i<drafts.length;i++){const d=drafts[i];const billing=d.party==='direct'?d.customerId:agents.find(a=>txt(a.id)===d.subAgentId)?.customer_id;if(!billing){setPlanError(true);setPlanMsg(`Order ${i+1}: select the billing party.`);return}if(d.party==='sub_agent'&&d.endMode==='existing'&&!d.endCustomerId){setPlanError(true);setPlanMsg(`Order ${i+1}: select the end customer.`);return}if(d.party==='sub_agent'&&d.endMode==='named'&&!d.endName.trim()){setPlanError(true);setPlanMsg(`Order ${i+1}: enter the end customer name.`);return}if(!d.lines.some((l:any)=>l.itemId&&Number(l.quantity)>0)){setPlanError(true);setPlanMsg(`Order ${i+1}: add at least one item.`);return}}
 const payload=drafts.map(d=>{const billing=d.party==='direct'?d.customerId:agents.find(a=>txt(a.id)===d.subAgentId)?.customer_id;return{customer_id:billing,order_party_type:d.party,sub_agent_id:d.party==='sub_agent'?d.subAgentId:null,end_customer_customer_id:d.party==='sub_agent'&&d.endMode==='existing'?d.endCustomerId:null,end_customer_name:d.party==='sub_agent'&&d.endMode==='named'?d.endName.trim():null,end_customer_phone:d.party==='sub_agent'?d.endPhone.trim()||null:null,end_customer_is_anonymous:d.party==='sub_agent'&&d.endMode==='anonymous',order_date:d.date,estimated_dispatch_date:d.dispatchDate||null,source:d.source,advance_amount:Number(d.advance||0),requests:d.requests.trim()||null,notes:d.notes.trim()||null,lines:d.lines.filter((l:any)=>l.itemId&&Number(l.quantity)>0).map((l:any)=>{const it=items.find(x=>txt(x.id)===l.itemId);return{item_id:l.itemId,ordered_quantity:Number(l.quantity),unit_id:it?.base_unit_id,selling_rate:Number(l.rate||0)}})}});
 setSaving(true);const{error}=await sb().rpc('create_order_entry_session',{p_orders:payload});setSaving(false);if(error){setPlanError(true);setPlanMsg(error.message);return}setOpen(false);setDrafts([blank()]);await loadOrders();setPlanError(false);setPlanMsg(`${drafts.length} order${drafts.length===1?'':'s'} received. Select an order below to prepare the plan and reserve stock.`)};
 const openPlan=async(o:Row)=>{setSelectedOrder(o);setPlanId(txt(o.id));setPlanMsg('');setPlanError(false);const[{data:ols},{data:inv},{data:rvs}]=await Promise.all([sb().from('order_lines').select('id,item_id,ordered_quantity,unit_id,selling_rate,reserved_quantity,produced_quantity,dispatched_quantity').eq('order_id',o.id),sb().from('v_inventory_current').select('*'),sb().from('recipe_versions').select('id,recipe_id,version_number,expected_output_quantity,output_unit_id,status,recipes!inner(output_item_id,name,status)').eq('status','active')]);const mapInv=new Map((inv??[]).map(x=>[txt(x.item_id),x]));const active=(rvs??[]).filter((x:any)=>x.recipes?.status==='active');setRecipes(active);setPlanLines((ols??[]).map((l:any)=>{const stock=mapInv.get(txt(l.item_id));const available=Math.max(0,Number(stock?.available_stock||0));const short=Math.max(0,Number(l.ordered_quantity)-Math.min(Number(l.ordered_quantity),available));const options=active.filter((r:any)=>txt(r.recipes?.output_item_id)===txt(l.item_id));return{...l,itemCode:items.find(i=>txt(i.id)===txt(l.item_id))?.item_code,itemName:items.find(i=>txt(i.id)===txt(l.item_id))?.name,available,shortfall:short,recipeVersionId:options.length===1?txt(options[0].id):'',recipeOptions:options}}))};
 const prepare=async()=>{const selections=planLines.filter(l=>Number(l.shortfall)>0).map(l=>({order_line_id:l.id,recipe_version_id:l.recipeVersionId}));if(planLines.some(l=>Number(l.shortfall)>0&&!l.recipeVersionId)){setPlanError(true);setPlanMsg('A recipe version must be selected for every item that needs production.');return}const{error}=await sb().rpc('prepare_order_plan',{p_order_id:planId,p_recipe_selections:selections});if(error){setPlanError(true);setPlanMsg(error.message);return}setPlanError(false);setPlanMsg('Production plan prepared and inventory reserved. The order is ready for confirmation.');await loadOrders();};
 const confirm=async()=>{const{error}=await sb().rpc('confirm_order',{p_order_id:planId});if(error){setPlanError(true);setPlanMsg(error.message);return}setPlanError(false);setPlanMsg('Order confirmed. Production work, if any, is now visible in Production.');await loadOrders();};
 const received=orders.filter(o=>['received','planned'].includes(txt(o.status)));return <Page title="Orders" description="Customer call → Order → production plan (recipe version when needed) → reserve stock → confirm → production → dispatch → Sale & Invoice." action="+ Create Order"><button className="primary-button" onClick={()=>{setOpen(true);setPlanMsg('');setPlanError(false)}}>+ Create Order</button>{open&&<Modal title="Create customer orders" close={()=>setOpen(false)} wide>{drafts.map((d,oi)=><div className="console-panel" key={oi} style={{marginBottom:16}}><div className="panel-heading"><h2>Order {oi+1}</h2><button className="secondary-button" onClick={()=>setDrafts(x=>x.length===1?x:x.filter((_,n)=>n!==oi))} disabled={drafts.length===1}>Remove</button></div><div className="form-grid"><Field label="Order type"><Select value={d.party} onChange={v=>patch(oi,'party',v)}><option value="direct">Direct customer</option><option value="sub_agent">Through Sub-Agent</option></Select></Field>{d.party==='direct'?<Field label="Bill To — Customer"><Select value={d.customerId} onChange={v=>patch(oi,'customerId',v)}><option value="">Select customer</option>{customers.map(c=><option key={txt(c.id)} value={txt(c.id)}>{txt(c.name)} — {txt(c.business_code)}</option>)}</Select></Field>:<Field label="Bill To — Sub-Agent"><Select value={d.subAgentId} onChange={v=>patch(oi,'subAgentId',v)}><option value="">Select Sub-Agent</option>{agents.map(a=><option key={txt(a.id)} value={txt(a.id)}>{txt(a.name)} — {txt(a.business_code)}</option>)}</Select></Field>}<Field label="Order date"><input type="date" value={d.date} onChange={e=>patch(oi,'date',e.target.value)}/></Field><Field label="Estimated dispatch date"><input type="date" value={d.dispatchDate} onChange={e=>patch(oi,'dispatchDate',e.target.value)}/></Field></div>{d.party==='sub_agent'&&<div className="form-grid"><Field label="End customer"><Select value={d.endMode} onChange={v=>patch(oi,'endMode',v)}><option value="existing">Existing customer</option><option value="named">Named but not registered</option><option value="anonymous">Anonymous</option></Select></Field>{d.endMode==='existing'&&<Field label="End customer record"><Select value={d.endCustomerId} onChange={v=>patch(oi,'endCustomerId',v)}><option value="">Select end customer</option>{customers.map(c=><option key={txt(c.id)} value={txt(c.id)}>{txt(c.name)}</option>)}</Select></Field>}{d.endMode==='named'&&<Field label="End customer name"><input value={d.endName} onChange={e=>patch(oi,'endName',e.target.value)}/></Field>}<Field label="End customer phone"><input value={d.endPhone} onChange={e=>patch(oi,'endPhone',e.target.value)}/></Field></div>}<div className="form-grid"><Field label="Order source"><Select value={d.source} onChange={v=>patch(oi,'source',v)}><option value="whatsapp">WhatsApp</option><option value="sms">SMS</option><option value="phone">Phone</option><option value="social_media">Social Media</option><option value="walk_in">Walk-in</option><option value="online_marketplace">Online Marketplace</option></Select></Field><Field label="Advance amount"><input type="number" min="0" step="0.01" value={d.advance} onChange={e=>patch(oi,'advance',e.target.value)}/></Field></div><h3>Items on this order</h3>{d.lines.map((l:any,li:number)=><div className="purchase-line" key={li}><Field label="Item"><Select value={l.itemId} onChange={v=>line(oi,li,'itemId',v)}><option value="">Select item</option>{items.filter(i=>i.is_active!==false&&i.can_be_sold!==false).map(i=><option key={txt(i.id)} value={txt(i.id)}>{txt(i.item_code)} — {txt(i.name)}</option>)}</Select></Field><Field label="Quantity"><input type="number" min="0" step="0.001" value={l.quantity} onChange={e=>line(oi,li,'quantity',e.target.value)}/></Field><Field label="Selling rate"><input type="number" min="0" step="0.01" value={l.rate} onChange={e=>line(oi,li,'rate',e.target.value)}/></Field></div>)}<button className="add-row-button" onClick={()=>patch(oi,'lines',[...d.lines,{itemId:'',quantity:'',rate:''}])}>＋ Add item row</button><div className="form-grid"><Field label="Requests"><textarea value={d.requests} onChange={e=>patch(oi,'requests',e.target.value)}/></Field><Field label="Notes"><textarea value={d.notes} onChange={e=>patch(oi,'notes',e.target.value)}/></Field></div></div>)}<button className="add-row-button" onClick={()=>setDrafts(x=>[...x,blank()])}>＋ Add another customer order</button><Status message={planMsg} error={planError}/><div className="purchase-actions"><button className="primary-button" onClick={save} disabled={saving}>{saving?'Saving…':'Save received orders'}</button></div></Modal>}
 <div className="console-panel" style={{marginTop:18}}><div className="panel-heading"><h2>Orders awaiting plan / confirmation</h2></div>{received.length?<div className="workflow-list">{received.map(o=><div className="workflow-row" key={txt(o.id)}><div><strong>{txt(o.business_code)}</strong><span>{txt(o.billing_customer_name)} · {txt(o.status)} · {txt(o.ordered_quantity)} units</span></div><button className="secondary-button" onClick={()=>openPlan(o)}>{o.status==='received'?'Prepare plan & reserve':'Open plan'}</button></div>)}</div>:<p className="table-message">No orders awaiting planning.</p>}</div>
 {selectedOrder&&<Modal title={`Plan order ${txt(selectedOrder.business_code)}`} close={()=>setSelectedOrder(null)} wide><p className="page-intro">The system reserves available finished stock. Any remaining quantity must have an active recipe version so the required production batch and ingredient plan can be created.</p>{planLines.map((l:any)=><div className="purchase-line" key={txt(l.id)}><div className="field"><span>Item</span><div><strong>{txt(l.itemCode)}</strong> — {txt(l.itemName)}</div></div><Field label="Ordered"><input value={txt(l.ordered_quantity)} readOnly/></Field><Field label="Available now"><input value={String(l.available)} readOnly/></Field><Field label="Production needed"><input value={String(l.shortfall)} readOnly/></Field>{Number(l.shortfall)>0&&<Field label="Recipe version"><Select value={l.recipeVersionId} onChange={v=>setPlanLines(x=>x.map(z=>z.id===l.id?{...z,recipeVersionId:v}:z))}><option value="">Select recipe version</option>{l.recipeOptions.map((r:any)=><option key={txt(r.id)} value={txt(r.id)}>v{txt(r.version_number)} — {txt(r.recipes?.name)}</option>)}</Select></Field>}</div>)}<Status message={planMsg} error={planError}/>{selectedOrder.status==='received'?<button className="primary-button" onClick={prepare}>Prepare plan & reserve inventory</button>:<button className="primary-button" onClick={confirm}>Confirm order</button>}</Modal>}
 <div className="console-panel" style={{marginTop:18}}><div className="panel-heading"><h2>All orders</h2></div><DataTable table="v_orders_list"/></div></Page>
}
function Inventory(){const[open,setOpen]=useState(false),[kind,setKind]=useState<'out'|'adjust'>('out'),[items]=useRows('items'),[units]=useRows('units'),[iid,setIid]=useState(''),[uid,setUid]=useState(''),[qty,setQty]=useState(''),[reason,setReason]=useState('damage'),[notes,setNotes]=useState(''),[msg,setMsg]=useState(''),[error,setError]=useState(false);const submit=async()=>{const n=Number(qty);if(!iid||!uid||!n){setError(true);setMsg('Item, unit and a non-zero quantity are required.');return}const rpc=kind==='out'?'record_stock_out':'record_stock_adjustment';const args=kind==='out'?{p_item_id:iid,p_quantity:n,p_unit_id:uid,p_stock_out_date:today(),p_reason:reason,p_notes:notes||null}:{p_item_id:iid,p_quantity_delta:n,p_unit_id:uid,p_adjustment_date:today(),p_reason:reason,p_notes:notes||null};const{error:e}=await sb().rpc(rpc,args);if(e){setError(true);setMsg(e.message);return}setError(false);setMsg(kind==='out'?'Stock Out recorded using FIFO.':'Stock Adjustment recorded.');setQty('');setNotes('');setOpen(false)};return <Page title="Inventory" description="Current stock comes from transactions. Reservations are commitments, not stock reductions." action="+ Stock Action"><button className="primary-button" onClick={()=>setOpen(true)}>+ Stock Action</button>{open&&<Modal title="Stock Action" close={()=>setOpen(false)}><div className="master-tabs"><button className={kind==='out'?'tab-active':''} onClick={()=>setKind('out')}>Stock Out</button><button className={kind==='adjust'?'tab-active':''} onClick={()=>setKind('adjust')}>Stock Adjustment</button></div><div className="form-grid"><Field label="Item"><Select value={iid} onChange={setIid}><option value="">Select item</option>{items.map(i=><option key={txt(i.id)} value={txt(i.id)}>{txt(i.item_code)} — {txt(i.name)}</option>)}</Select></Field><Field label="Unit"><Select value={uid} onChange={setUid}><option value="">Select unit</option>{units.map(u=><option key={txt(u.id)} value={txt(u.id)}>{txt(u.name)} ({txt(u.symbol)})</option>)}</Select></Field><Field label={kind==='out'?'Quantity':'Adjustment (+ adds / − removes)'}><input type="number" step="0.001" value={qty} onChange={e=>setQty(e.target.value)}/></Field><Field label="Reason"><Select value={reason} onChange={setReason}><option value="damage">Damage</option><option value="expiry">Expiry</option><option value="sample">Sample</option><option value="wastage">Wastage</option><option value="personal_use">Personal use</option><option value="production">Production</option><option value="correction">Correction</option><option value="other">Other</option></Select></Field></div><Field label="Notes"><textarea value={notes} onChange={e=>setNotes(e.target.value)}/></Field><Status message={msg} error={error}/><button className="primary-button" onClick={submit}>Record</button></Modal>}<div className="console-panel" style={{marginTop:18}}><div className="panel-heading"><h2>Current stock</h2></div><DataTable table="v_inventory_current"/></div><div className="console-panel" style={{marginTop:18}}><div className="panel-heading"><h2>Inventory movements</h2></div><DataTable table="inventory_transactions"/></div></Page>}
function Production(){const[batches,load]=useRows('v_production_batches_list'),[items]=useRows('items'),[open,setOpen]=useState(false),[bid,setBid]=useState(''),[cons,setCons]=useState<any[]>([]),[out,setOut]=useState(''),[msg,setMsg]=useState(''),[error,setError]=useState(false),[selected,setSelected]=useState<Row|null>(null);const choose=async(id:string)=>{setBid(id);const b=batches.find(x=>txt(x.id)===id);setSelected(b??null);setOut(txt(b?.planned_output_quantity));if(!id){setCons([]);return}const{data}=await sb().from('production_batch_plan_lines').select('id,ingredient_item_id,planned_quantity,unit_id').eq('production_batch_id',id).order('id');setCons((data??[]).map((x:any)=>({id:x.id,itemId:txt(x.ingredient_item_id),unitId:txt(x.unit_id),planned:txt(x.planned_quantity),actual:txt(x.planned_quantity)})))};const approve=async()=>{const{data:u}=await sb().auth.getUser();const{error:e}=await sb().from('production_batches').update({wife_approval_status:'approved',approved_by:u.user?.id??null,approved_at:new Date().toISOString()}).eq('id',bid);if(e){setError(true);setMsg(e.message);return}setError(false);setMsg('Production batch approved.');await load()};const start=async()=>{const{error:e}=await sb().rpc('start_production_batch',{p_production_batch_id:bid});if(e){setError(true);setMsg(e.message);return}setError(false);setMsg('Production started.');await load()};const complete=async()=>{if(!selected||!bid)return;const c=cons.filter(x=>Number(x.actual)>0).map(x=>({ingredient_item_id:x.itemId,actual_quantity:Number(x.actual),unit_id:x.unitId}));if(!c.length||Number(out)<=0){setError(true);setMsg('Enter actual consumption and output.');return}const{error:e}=await sb().rpc('complete_production',{p_production_batch_id:bid,p_consumptions:c,p_outputs:[{output_item_id:selected.output_item_id,quantity:Number(out),unit_id:selected.output_unit_id}]});if(e){setError(true);setMsg(e.message);return}setError(false);setMsg('Production completed. Ingredient stock consumed and finished stock created.');setOpen(false);await load()};return <Page title="Production" description="Order-driven production shows exactly what must be produced, the selected recipe version, planned ingredients, approval, batch and actual consumption." action="+ Review Production"><button className="primary-button" onClick={()=>setOpen(true)}>+ Review Production</button>{open&&<Modal title="Production batch" close={()=>setOpen(false)} wide><Field label="Production batch"><Select value={bid} onChange={choose}><option value="">Select planned batch</option>{batches.filter(b=>txt(b.status)!=='completed').map(b=><option key={txt(b.id)} value={txt(b.id)}>{txt(b.business_code)} — {txt(b.order_code)} — {txt(b.output_item_name)} — {txt(b.status)}</option>)}</Select></Field>{selected&&<><div className="console-panel"><div className="panel-heading"><h2>{txt(selected.output_item_code)} — {txt(selected.output_item_name)}</h2></div><p className="page-intro">Order: {txt(selected.order_code)} · Recipe version: {txt(selected.recipe_version)||'not selected'}</p><div className="form-grid"><Field label="Planned output"><input value={txt(selected.planned_output_quantity)} readOnly/></Field><Field label="Actual output"><input type="number" min="0" step="0.001" value={out} onChange={e=>setOut(e.target.value)}/></Field></div><h3>Planned ingredients</h3>{cons.map((x,i)=><div className="purchase-line" key={x.id}><div className="field"><span>Ingredient</span><div>{txt(items.find(it=>txt(it.id)===x.itemId)?.item_code)} — {txt(items.find(it=>txt(it.id)===x.itemId)?.name)}</div></div><Field label="Planned"><input value={x.planned} readOnly/></Field><Field label="Actual"><input type="number" min="0" step="0.001" value={x.actual} onChange={e=>setCons(v=>v.map((z,n)=>n===i?{...z,actual:e.target.value}:z))}/></Field></div>)}<Status message={msg} error={error}/><div className="purchase-actions"><button className="secondary-button" onClick={approve} disabled={selected.wife_approval_status==='approved'}>{selected.wife_approval_status==='approved'?'Approved':'Approve production'}</button><button className="secondary-button" onClick={start} disabled={selected.status!=='planned'||selected.wife_approval_status!=='approved'}>Start production</button><button className="primary-button" onClick={complete} disabled={!['planned','in_progress'].includes(txt(selected.status))||selected.wife_approval_status!=='approved'}>Complete production</button></div></div></>}</Modal>}<div className="console-panel"><div className="panel-heading"><h2>Production batches</h2></div><DataTable table="v_production_batches_list"/></div></Page>}
function Sales(){const[orders]=useRows('orders'),[items]=useRows('items'),[open,setOpen]=useState(false),[oid,setOid]=useState(''),[method,setMethod]=useState('delivery'),[lines,setLines]=useState<any[]>([]),[msg,setMsg]=useState(''),[error,setError]=useState(false);const loadLines=async(id:string)=>{setOid(id);const{data}=await sb().from('order_lines').select('id,item_id,ordered_quantity,dispatched_quantity,unit_id,selling_rate').eq('order_id',id);setLines((data??[]).map((x:any)=>({...x,dispatchQty:String(Math.max(0,Number(x.ordered_quantity)-Number(x.dispatched_quantity||0)))})))};const dispatchInvoice=async()=>{const selected=lines.filter(x=>Number(x.dispatchQty)>0);if(!oid||!selected.length){setError(true);setMsg('Select an order and at least one quantity.');return}const{error:e}=await sb().rpc('dispatch_and_invoice_order',{p_order_id:oid,p_dispatch_date:today(),p_method:method,p_lines:selected.map(x=>({order_line_id:x.id,quantity:Number(x.dispatchQty)}))});if(e){setError(true);setMsg(e.message);return}setError(false);setMsg('Dispatch completed. Sale and Invoice were raised from that actual dispatch.');setLines([]);setOid('')};return <Page title="Sales & Invoices" description="Dispatch is the physical handover. On completion, the system creates the Sale and Invoice from that exact dispatch — no re-entry." action="+ Dispatch + Sale + Invoice"><button className="primary-button" onClick={()=>setOpen(true)}>+ Dispatch + Sale + Invoice</button>{open&&<Modal title="Dispatch → Sale → Invoice" close={()=>setOpen(false)} wide><div className="form-grid"><Field label="Order"><Select value={oid} onChange={loadLines}><option value="">Select confirmed order</option>{orders.filter(o=>['confirmed','in_production','ready','partially_dispatched'].includes(txt(o.status))).map(o=><option key={txt(o.id)} value={txt(o.id)}>{txt(o.business_code)} — {txt(o.billing_customer_name)} — {txt(o.status)}</option>)}</Select></Field><Field label="Dispatch method"><Select value={method} onChange={setMethod}><option value="pickup">Pickup</option><option value="delivery">Delivery</option><option value="courier">Courier</option><option value="other">Other</option></Select></Field></div>{lines.map((x,i)=><div className="purchase-line" key={txt(x.id)}><div className="field"><span>Item</span><div>{txt(items.find(it=>txt(it.id)===txt(x.item_id))?.item_code)} — {txt(items.find(it=>txt(it.id)===txt(x.item_id))?.name)}</div></div><Field label="Remaining"><input value={String(Math.max(0,Number(x.ordered_quantity)-Number(x.dispatched_quantity||0)))} readOnly/></Field><Field label="Dispatch quantity"><input type="number" min="0" step="0.001" value={x.dispatchQty} onChange={e=>setLines(v=>v.map((z,n)=>n===i?{...z,dispatchQty:e.target.value}:z))}/></Field></div>)}<Status message={msg} error={error}/><button className="primary-button" onClick={dispatchInvoice}>Dispatch + Sale + Invoice</button></Modal>}<div className="console-grid"><div className="console-panel"><div className="panel-heading"><h2>Dispatches</h2></div><DataTable table="dispatches"/></div><div className="console-panel"><div className="panel-heading"><h2>Sales</h2></div><DataTable table="sales"/></div><div className="console-panel"><div className="panel-heading"><h2>Invoices</h2></div><DataTable table="invoices"/></div></div></Page>}
function Payments(){const[customers]=useRows('customers'),[suppliers]=useRows('suppliers'),[open,setOpen]=useState(false),[dir,setDir]=useState<'customer'|'supplier'>('customer'),[party,setParty]=useState(''),[amount,setAmount]=useState(''),[method,setMethod]=useState('upi'),[ref,setRef]=useState(''),[msg,setMsg]=useState(''),[error,setError]=useState(false);const submit=async()=>{if(!party||Number(amount)<=0){setError(true);setMsg('Select the party and enter a positive amount.');return}const rpc=dir==='customer'?'record_customer_payment':'record_supplier_payment';const args=dir==='customer'?{p_customer_id:party,p_amount:Number(amount),p_payment_date:today(),p_method:method,p_invoice_id:null,p_upi_reference:ref||null,p_notes:msg||null}:{p_supplier_id:party,p_amount:Number(amount),p_payment_date:today(),p_method:method,p_purchase_id:null,p_upi_reference:ref||null,p_notes:msg||null};const{error:e}=await sb().rpc(rpc,args);if(e){setError(true);setMsg(e.message);return}setError(false);setMsg('Payment recorded.');setAmount('');setRef('')};return <Page title="Payments" description="Customer receipts and supplier payments are separate money directions. Document allocation and advances remain explicit." action="+ Record Payment"><button className="primary-button" onClick={()=>setOpen(true)}>+ Record Payment</button>{open&&<Modal title={dir==='customer'?'Receive Customer Payment':'Pay Supplier'} close={()=>setOpen(false)}><div className="master-tabs"><button className={dir==='customer'?'tab-active':''} onClick={()=>{setDir('customer');setParty('')}}>Customer Receipt</button><button className={dir==='supplier'?'tab-active':''} onClick={()=>{setDir('supplier');setParty('')}}>Supplier Payment</button></div><Field label={dir==='customer'?'Customer':'Supplier'}><Select value={party} onChange={setParty}><option value="">Select {dir}</option>{(dir==='customer'?customers:suppliers).map(x=><option key={txt(x.id)} value={txt(x.id)}>{txt(x.name||x.business_name)} — {txt(x.business_code)}</option>)}</Select></Field><div className="form-grid"><Field label="Amount"><input type="number" min="0.01" step="0.01" value={amount} onChange={e=>setAmount(e.target.value)}/></Field><Field label="Method"><Select value={method} onChange={setMethod}><option value="upi">UPI</option><option value="cash">Cash</option><option value="bank_transfer">Bank transfer</option><option value="other">Other</option></Select></Field><Field label="Payment reference"><input value={ref} onChange={e=>setRef(e.target.value)}/></Field></div><Status message={msg} error={error}/><button className="primary-button" onClick={submit}>Record {dir==='customer'?'Receipt':'Payment'}</button></Modal>}<div className="console-grid"><div className="console-panel"><div className="panel-heading"><h2>Customer receipts</h2></div><DataTable table="customer_payments"/></div><div className="console-panel"><div className="panel-heading"><h2>Supplier payments</h2></div><DataTable table="supplier_payments"/></div></div></Page>}
export default function OperationsConsoleFinal2({module}:{module:Module}){if(module==='orders')return <Orders/>;if(module==='inventory')return <Inventory/>;if(module==='sales')return <Sales/>;if(module==='payments')return <Payments/>;return <Production/>}
