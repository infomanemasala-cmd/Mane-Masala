'use client'

import {useEffect,useMemo,useState} from 'react'
import {createClient} from '@/lib/supabase/client'
import DataTable from '@/components/data-table'

type R=Record<string,any>
const db=()=>createClient();const s=(v:any)=>String(v??'');const n=(v:any)=>Number(v??0);const today=()=>new Date().toISOString().slice(0,10)
function Field({label,help,children}:{label:string;help?:string;children:React.ReactNode}){return <label className="field"><span>{label}</span>{children}{help&&<small>{help}</small>}</label>}
function Status({message,error}:{message:string;error:boolean}){return message?<p className={`form-status${error?' form-status-error':''}`} role="alert">{message}</p>:null}
function Action({children,primary=false,disabled=false,onClick}:{children:React.ReactNode;primary?:boolean;disabled?:boolean;onClick:()=>void}){return <button className={primary?'primary-button':'secondary-button'} disabled={disabled} onClick={onClick}>{children}</button>}

export default function OrdersConsoleV4({partialOnly=false}:{partialOnly?:boolean}){
 const[orders,setOrders]=useState<R[]>([]),[customers,setCustomers]=useState<R[]>([]),[items,setItems]=useState<R[]>([]),[units,setUnits]=useState<R[]>([]),[recipes,setRecipes]=useState<R[]>([]),[versions,setVersions]=useState<R[]>([])
 const[selected,setSelected]=useState<R|null>(null),[lines,setLines]=useState<R[]>([]),[batches,setBatches]=useState<R[]>([]),[plans,setPlans]=useState<Record<string,R[]>>({}),[proc,setProc]=useState<R[]>([]),[stock,setStock]=useState<Record<string,R>>({})
 const[choice,setChoice]=useState<Record<string,string>>({}),[extra,setExtra]=useState<Record<string,R>>({}),[partialQty,setPartialQty]=useState<Record<string,string>>({}),[actual,setActual]=useState<Record<string,string>>({}),[dispatchQty,setDispatchQty]=useState<Record<string,string>>({})
 const[newOrder,setNewOrder]=useState(false),[customer,setCustomer]=useState(''),[orderDate,setOrderDate]=useState(today()),[source,setSource]=useState('phone'),[advance,setAdvance]=useState(''),[advanceMethod,setAdvanceMethod]=useState('upi'),[orderLines,setOrderLines]=useState<R[]>([{itemId:'',qty:'',rate:''}])
 const[dispatchDate,setDispatchDate]=useState(''),[dispatchMethod,setDispatchMethod]=useState('customer_pickup'),[msg,setMsg]=useState(''),[err,setErr]=useState(false),[busy,setBusy]=useState(''),[refresh,setRefresh]=useState(0)

 const load=async()=>{const x=db();const[c,i,u,r,v,o,iv]=await Promise.all([x.from('customers').select('*').eq('is_active',true).order('name'),x.from('items').select('*').eq('is_active',true).order('name'),x.from('units').select('*').eq('is_active',true).order('name'),x.from('recipes').select('id,name,output_item_id,status').eq('status','active'),x.from('recipe_versions').select('*').eq('status','active').order('version_number'),x.from('v_orders_list').select('*').order('order_date',{ascending:false}),x.from('v_inventory_current').select('*')]);setCustomers(c.data??[]);setItems(i.data??[]);setUnits(u.data??[]);setRecipes(r.data??[]);setVersions(v.data??[]);setOrders(o.data??[]);const sm:Record<string,R>={};for(const z of iv.data??[])sm[s(z.item_id)]=z;setStock(sm)}
 useEffect(()=>{void load()},[])
 const unit=(id:string)=>s(units.find(x=>s(x.id)===id)?.symbol||units.find(x=>s(x.id)===id)?.name||id)
 const item=(id:string)=>items.find(x=>s(x.id)===id)
 const recipeName=(id:string)=>{const v=versions.find(x=>s(x.id)===id),r=recipes.find(x=>s(x.id)===s(v?.recipe_id));return v&&r?`${s(r.name)} — v${s(v.version_number)}`:'Not selected'}
 const availableRecipes=(itemId:string)=>versions.filter(v=>recipes.some(r=>s(r.id)===s(v.recipe_id)&&s(r.output_item_id)===itemId))

 const openOrder=async(o:R)=>{setSelected(o);setDispatchDate(s(o.estimated_dispatch_date)||'');setMsg('');setErr(false);const x=db();const[a,b,q,p,iv]=await Promise.all([x.from('order_lines').select('*').eq('order_id',o.id).order('created_at'),x.from('v_production_batches_list').select('*').eq('order_id',o.id).order('production_date'),x.from('order_line_procurement_requirements').select('*').eq('order_id',o.id),x.from('production_batch_plan_lines').select('*').in('production_batch_id',(await x.from('production_batches').select('id').eq('order_id',o.id)).data?.map(z=>z.id)||[]).order('created_at'),x.from('v_inventory_current').select('*')]);if(a.error||b.error||q.error||p.error||iv.error){setErr(true);setMsg(a.error?.message||b.error?.message||q.error?.message||p.error?.message||iv.error?.message||'Unable to load order');return}const sm:Record<string,R>={};for(const z of iv.data??[])sm[s(z.item_id)]=z;setStock(sm);setLines((a.data??[]).map(l=>({...l,item:item(s(l.item_id)),stock:sm[s(l.item_id)]||{current_stock:0,available_stock:0}})));setBatches(b.data??[]);setProc(q.data??[]);const pm:Record<string,R[]>={};for(const z of p.data??[])pm[s(z.production_batch_id)]=[...(pm[s(z.production_batch_id)]??[]),z];setPlans(pm);const pq:Record<string,string>={};for(const z of b.data??[])pq[s(z.id)]=s(z.planned_output_quantity);setPartialQty(pq);const aq:Record<string,string>={};for(const z of b.data??[])aq[s(z.id)]=s(z.actual_output_quantity||z.planned_output_quantity);setActual(aq)}

 const act=async(key:string,fn:()=>any,success:string)=>{setBusy(key);const r=await fn();setBusy('');if(r?.error){setErr(true);setMsg(r.error.message);return false}setErr(false);setMsg(success);if(selected)await openOrder(selected);await load();setRefresh(x=>x+1);return true}
 const saveOrder=async()=>{const valid=orderLines.filter(l=>l.itemId&&n(l.qty)>0);if(!customer||!valid.length){setErr(true);setMsg('Choose a customer and add at least one item.');return}if(valid.some(l=>n(l.rate)<0)){setErr(true);setMsg('Selling rate cannot be negative.');return}await act('new',()=>db().rpc('create_order_entry_session',{p_orders:[{customer_id:customer,order_party_type:'direct',order_date:orderDate,source,advance_amount:n(advance),advance_payment_method:n(advance)>0?advanceMethod:null,lines:valid.map(l=>({item_id:l.itemId,ordered_quantity:n(l.qty),selling_rate:n(l.rate)}))}]}),'Order received.');setNewOrder(false);setCustomer('');setOrderLines([{itemId:'',qty:'',rate:''}]);setAdvance('')}
 const prepare=async()=>{if(!selected)return;const need=lines.filter(l=>n(l.ordered_quantity)>Math.min(n(l.ordered_quantity),n(l.stock?.available_stock)));if(need.some(l=>!choice[s(l.id)])){setErr(true);setMsg('Select a recipe for every item that needs production.');return}await act('prepare',()=>db().rpc('prepare_order_plan',{p_order_id:selected.id,p_recipe_selections:need.map(l=>({order_line_id:l.id,recipe_version_id:choice[s(l.id)]}))}),'Production plan prepared. Now review each item.');}
 const approve=async(b:R)=>act('approve'+b.id,()=>db().rpc('approve_production_batch',{p_production_batch_id:b.id}),'Item approved.');
 const partial=async(b:R)=>{const q=n(partialQty[s(b.id)]);if(q<=0||q>=n(b.planned_output_quantity)){setErr(true);setMsg('For partial approval, enter a quantity less than the production target.');return}await act('partial'+b.id,()=>db().rpc('partial_approve_production_batch',{p_production_batch_id:b.id,p_approved_output:q}),`Approved ${q} ${unit(s(b.output_unit_id))} for this item. Remaining quantity stays pending.`)}
 const procure=async(b:R)=>act('proc'+b.id,()=>db().rpc('mark_production_pending_procurement',{p_production_batch_id:b.id}),'Purchase required recorded.');
 const revise=async(b:R)=>{const e=extra[s(b.id)]||{};const rv=s(e.recipe||b.recipe_version_id);const add=e.item&&n(e.qty)>0?[{ingredient_item_id:e.item,quantity:n(e.qty),unit_id:item(e.item)?.base_unit_id}]:[];if(!rv){setErr(true);setMsg('Choose a recipe version.');return}await act('rev'+b.id,()=>db().rpc('revise_production_plan',{p_production_batch_id:b.id,p_recipe_version_id:rv,p_additional_ingredients:add,p_permanent_recipe_change:e.mode==='permanent'}),e.mode==='permanent'?'New recipe version saved.':'This production plan was updated.');setExtra(z=>({...z,[s(b.id)]:{}}))}
 const confirm=async()=>{if(!selected)return;await act('confirm',()=>db().rpc('confirm_order',{p_order_id:selected.id}),'Order confirmed. Approved items can now be produced.');}
 const start=async(b:R)=>act('start'+b.id,()=>db().rpc('start_production_batch',{p_production_batch_id:b.id}),'Production started.');
 const complete=async(b:R)=>{const ps=plans[s(b.id)]??[];const consumptions=ps.map(p=>({ingredient_item_id:p.ingredient_item_id,actual_quantity:n(actual['c'+p.id]??p.planned_quantity),unit_id:p.unit_id}));const output=n(actual[s(b.id)]);if(output<=0){setErr(true);setMsg('Enter actual finished quantity.');return}await act('complete'+b.id,()=>db().rpc('complete_production',{p_production_batch_id:b.id,p_consumptions:consumptions,p_outputs:[{output_item_id:b.output_item_id,quantity:output,unit_id:b.output_unit_id,unit_cost:0}]}),'Production completed and finished stock updated.');}
 const dispatch=async()=>{if(!selected)return;const dl=lines.map(l=>({order_line_id:l.id,quantity:n(dispatchQty[s(l.id)]||0)})).filter(x=>x.quantity>0);if(!dl.length){setErr(true);setMsg('Enter at least one quantity to dispatch.');return}await act('dispatch',()=>db().rpc('dispatch_order',{p_order_id:selected.id,p_dispatch_date:dispatchDate||today(),p_method:dispatchMethod,p_lines:dl}),'Dispatch recorded.');}
 const saveDate=async()=>{if(!selected||!dispatchDate)return;await act('date',()=>db().rpc('set_order_estimated_dispatch_date',{p_order_id:selected.id,p_estimated_dispatch_date:dispatchDate}),'Dispatch date saved.');}
 const openOrders=useMemo(()=>orders.filter(o=>!['dispatched','cancelled'].includes(s(o.status))),[orders])
 const productionPending=batches.some(b=>s(b.wife_approval_status)!=='approved'||s(b.status)!=='completed')

 return <section className="page-panel"><div className="section-label">Mane Masala</div><div className="master-header"><div><h1>{partialOnly?'Order Approval':'Orders'}</h1><p className="page-intro">One simple flow: <strong>Order → Check stock → Review production → Approve → Produce → Dispatch</strong>.</p></div>{!partialOnly&&<button className="primary-button" onClick={()=>{setNewOrder(true);setErr(false);setMsg('')}}>+ Create Order</button>}</div>
 {!partialOnly&&<div className="console-panel"><div className="panel-heading"><div><h2>What needs attention</h2><span>Open an order to continue from its current step.</span></div><span className="status-pill">{openOrders.length} open</span></div>{openOrders.slice(0,8).map(o=><div className="workflow-row" key={s(o.id)}><div><a className="table-link" href="#" onClick={e=>{e.preventDefault();void openOrder(o)}}>{s(o.business_code)}</a><span>{s(o.billing_customer_name)} · {s(o.status)}</span></div><button className="secondary-button" onClick={()=>void openOrder(o)}>Open order</button></div>)}{!openOrders.length&&<p className="table-message">No open orders.</p>}</div>}
 {!partialOnly&&<div className="console-panel"><div className="panel-heading"><h2>All orders</h2><span>Business Code opens the complete transaction.</span></div><DataTable table="v_orders_list" refreshToken={refresh} linkColumns={['business_code']} onRowOpen={openOrder}/></div>}
 {newOrder&&<div className="modal-backdrop"><div className="modal-card purchase-modal" style={{maxWidth:1100,maxHeight:'calc(100vh - 24px)',overflow:'auto'}}><div className="modal-header"><div><h3>New customer order</h3><span>Enter the customer request once, then save.</span></div><button className="secondary-button" onClick={()=>setNewOrder(false)}>Close</button></div><div className="form-grid"><Field label="Customer" help="Search/select the customer name."><select value={customer} onChange={e=>setCustomer(e.target.value)}><option value="">Select customer...</option>{customers.map(c=><option key={s(c.id)} value={s(c.id)}>{s(c.name)} — {s(c.business_code)}</option>)}</select></Field><Field label="Order date"><input type="date" value={orderDate} onChange={e=>setOrderDate(e.target.value)}/></Field><Field label="How was the order received?"><select value={source} onChange={e=>setSource(e.target.value)}><option value="phone">Phone</option><option value="whatsapp">WhatsApp</option><option value="walk_in">Walk-in</option><option value="sms">SMS</option><option value="social_media">Social Media</option><option value="online_marketplace">Online Marketplace</option></select></Field></div><div className="section-divider"/><div className="panel-heading"><h4>Items requested</h4><span>One row for each finished product.</span></div><div className="table-wrap"><table><thead><tr><th>Finished product</th><th>Quantity</th><th>Selling rate</th><th></th></tr></thead><tbody>{orderLines.map((l,i)=><tr key={i}><td><select value={l.itemId} onChange={e=>setOrderLines(v=>v.map((x,j)=>j===i?{...x,itemId:e.target.value}:x))}><option value="">Select product...</option>{items.filter(x=>x.can_be_sold!==false).map(x=><option key={s(x.id)} value={s(x.id)}>{s(x.item_code||x.business_code)} — {s(x.name)}</option>)}</select></td><td><input type="number" min="0" step="0.001" value={l.qty} placeholder="0.000 — quantity" onChange={e=>setOrderLines(v=>v.map((x,j)=>j===i?{...x,qty:e.target.value}:x))}/></td><td><input type="number" min="0" step="0.01" value={l.rate} placeholder="₹ 0.00 — per unit" onChange={e=>setOrderLines(v=>v.map((x,j)=>j===i?{...x,rate:e.target.value}:x))}/></td><td><button className="line-remove" disabled={orderLines.length===1} onClick={()=>setOrderLines(v=>v.filter((_,j)=>j!==i))}>Remove</button></td></tr>)}</tbody></table></div><button className="add-row-button" onClick={()=>setOrderLines(v=>[...v,{itemId:'',qty:'',rate:''}])}>+ Add another product</button><div className="form-grid" style={{marginTop:14}}><Field label="Advance received" help="Leave 0 if no advance was received."><input type="number" min="0" step="0.01" value={advance} placeholder="₹ 0.00" onChange={e=>setAdvance(e.target.value)}/></Field>{n(advance)>0&&<Field label="Payment method"><select value={advanceMethod} onChange={e=>setAdvanceMethod(e.target.value)}><option value="upi">UPI</option><option value="cash">Cash</option><option value="other">Other</option></select></Field>}</div><div className="workflow-actions" style={{position:'sticky',bottom:0,background:'inherit',paddingTop:12}}><Action primary disabled={busy==='new'} onClick={()=>void saveOrder()}>{busy==='new'?'Saving…':'Save order'}</Action></div><Status message={msg} error={err}/></div></div>}

 {selected&&<div className="modal-backdrop"><div className="modal-card purchase-modal" style={{maxWidth:1180,maxHeight:'calc(100vh - 24px)',overflow:'auto'}}><div className="modal-header" style={{position:'sticky',top:0,zIndex:3,background:'inherit'}}><div><h3>Order {s(selected.business_code)}</h3><span>{s(selected.billing_customer_name)} · {s(selected.status)}</span></div><button className="secondary-button" onClick={()=>setSelected(null)}>Close</button></div>
 <div className="workflow-row"><div><strong>Step 1 · Check the order</strong><span>Confirm customer, quantity and dispatch date.</span></div><div className="workflow-actions"><Field label="Estimated dispatch date"><input type="date" min={s(selected.order_date)} value={dispatchDate} onChange={e=>setDispatchDate(e.target.value)}/></Field><Action disabled={!dispatchDate||busy==='date'} onClick={()=>void saveDate()}>Save date</Action></div></div>
 <div className="console-panel"><div className="panel-heading"><div><h3>Items in this order</h3><span>Each item is handled separately.</span></div><span className="status-pill">{lines.length} item(s)</span></div><div className="table-wrap"><table><thead><tr><th>Product</th><th>Ordered</th><th>Finished stock</th><th>Need to produce</th><th>Status</th></tr></thead><tbody>{lines.map(l=>{const need=Math.max(0,n(l.ordered_quantity)-Math.min(n(l.ordered_quantity),n(l.stock?.available_stock)));return <tr key={s(l.id)}><td><strong>{s(l.item?.business_code||l.item?.item_code)}</strong><br/>{s(l.item?.name)}</td><td className="num">{n(l.ordered_quantity)} {unit(s(l.unit_id))}</td><td className="num">{n(l.stock?.available_stock)} {unit(s(l.unit_id))}</td><td className="num">{need} {unit(s(l.unit_id))}</td><td><span className="status-pill">{s(l.production_approval_status||'Stock available')}</span></td></tr>})}</tbody></table></div></div>
 <div className="console-panel"><div className="panel-heading"><div><h3>Step 2 · Production review</h3><span>Only the items that need production require a decision.</span></div><span className="status-pill">{batches.length} plan(s)</span></div>{batches.length===0?<div className="workflow-actions"><span>No production plan yet.</span><Action primary disabled={busy==='prepare'} onClick={()=>void prepare()}>Prepare production plan</Action></div>:batches.map(b=>{const id=s(b.id),l=lines.find(x=>s(x.id)===s(b.order_line_id)),ps=plans[id]??[],qs=proc.filter(q=>s(q.production_batch_id)===id&&s(q.status)==='open'),status=s(b.wife_approval_status),e=extra[id]||{};return <div className="console-panel" key={id} style={{marginTop:12}}><div className="panel-heading"><div><h4>{s(l?.item?.business_code||l?.item?.item_code)} — {s(l?.item?.name)}</h4><span>Production target: {n(b.planned_output_quantity)} {unit(s(b.output_unit_id))} · Recipe: {recipeName(s(b.recipe_version_id))}</span></div><span className="status-pill">{status}</span></div><div className="table-wrap"><table><thead><tr><th>Raw material</th><th>Needed</th><th>Current stock</th><th>Reserved</th><th>Net available</th></tr></thead><tbody>{ps.map(p=>{const st=stock[s(p.ingredient_item_id)]||{};const r=proc.find(q=>s(q.production_batch_id)===id&&s(q.ingredient_item_id)===s(p.ingredient_item_id));const reserved=n(r?.available_quantity);return <tr key={s(p.id)}><td>{s(item(s(p.ingredient_item_id))?.business_code)} — {s(item(s(p.ingredient_item_id))?.name)}</td><td className="num">{n(p.planned_quantity)} {unit(s(p.unit_id))}</td><td className="num">{n(st.current_stock)} {unit(s(p.unit_id))}</td><td className="num">{reserved} {unit(s(p.unit_id))}</td><td className="num">{Math.max(0,n(st.current_stock)-reserved)} {unit(s(p.unit_id))}</td></tr>})}</tbody></table></div>{qs.length>0&&<div className="alert-panel"><strong>Purchase required before approval</strong>{qs.map(q=><div className="workflow-row" key={s(q.id)}><div><strong>{s(item(s(q.ingredient_item_id))?.name)}</strong><span>Buy {n(q.shortfall_quantity)} {unit(s(q.unit_id))} now.</span></div><span className="status-pill">URGENT</span></div>)}<Action disabled={busy==='proc'+id} onClick={()=>void procure(b)}>Mark purchase required</Action></div>}<div className="section-divider"/><div className="panel-heading"><div><h4>Recipe change for this production</h4><span>Leave unchanged if no change is needed.</span></div></div><div className="form-grid"><Field label="Recipe"><select value={s(e.recipe||b.recipe_version_id)} onChange={x=>setExtra(z=>({...z,[id]:{...e,recipe:x.target.value}}))}>{availableRecipes(s(l?.item_id)).map(v=><option key={s(v.id)} value={s(v.id)}>{recipeName(s(v.id))}</option>)}</select></Field><Field label="Additional raw material"><select value={s(e.item)} onChange={x=>setExtra(z=>({...z,[id]:{...e,item:x.target.value}}))}><option value="">None</option>{items.map(i=><option key={s(i.id)} value={s(i.id)}>{s(i.business_code||i.item_code)} — {s(i.name)}</option>)}</select></Field><Field label="Additional quantity" help="Use the selected material's standard unit."><input type="number" min="0" step="0.001" value={s(e.qty)} placeholder="0.000" onChange={x=>setExtra(z=>({...z,[id]:{...e,qty:x.target.value}}))}/></Field><Field label="Save change"><select value={s(e.mode||'one_time')} onChange={x=>setExtra(z=>({...z,[id]:{...e,mode:x.target.value}}))}><option value="one_time">This production only</option><option value="permanent">Save as new recipe version</option></select></Field></div><Action disabled={busy==='rev'+id} onClick={()=>void revise(b)}>Update plan</Action><div className="workflow-actions">{qs.length?null:status!=='approved'&&<><Action primary disabled={busy==='approve'+id} onClick={()=>void approve(b)}>Approve this item</Action><span>or</span><input className="compact-number" type="number" min="0" max={Math.max(0,n(b.planned_output_quantity)-0.001)} step="0.001" value={s(partialQty[id])} onChange={e=>setPartialQty(z=>({...z,[id]:e.target.value}))} placeholder="Partial qty"/><Action disabled={busy==='partial'+id} onClick={()=>void partial(b)}>Approve partially</Action></>}</div></div>})}</div>
 {batches.length>0&&<div className="workflow-actions"><Action primary disabled={busy==='confirm'} onClick={()=>void confirm()}>Confirm approved items</Action><span>Only approved items are allowed to proceed.</span></div>}
 <div className="console-panel"><div className="panel-heading"><div><h3>Step 3 · Production</h3><span>Start only after approval. Record what was actually used and produced.</span></div></div>{batches.map(b=>{const id=s(b.id),ps=plans[id]??[],approved=s(b.wife_approval_status)==='approved',started=s(b.status)==='in_progress',done=s(b.status)==='completed';return <div className="workflow-row" key={id}><div><strong>{s(b.business_code)}</strong><span>{s(b.status)} · {s(b.wife_approval_status)}</span></div>{approved&&s(b.status)==='planned'&&<Action disabled={busy==='start'+id} onClick={()=>void start(b)}>Start production</Action>}{started&&<div className="workflow-actions"><span>Actual ingredients:</span>{ps.map(p=><label key={s(p.id)}><small>{s(item(s(p.ingredient_item_id))?.name)}</small><input className="compact-number" type="number" min="0" step="0.001" value={s(actual['c'+p.id]??p.planned_quantity)} onChange={e=>setActual(z=>({...z,['c'+p.id]:e.target.value}))}/></label>)}<label><small>Finished quantity</small><input className="compact-number" type="number" min="0" step="0.001" value={s(actual[id]??b.planned_output_quantity)} onChange={e=>setActual(z=>({...z,[id]:e.target.value}))}/></label><Action primary disabled={busy==='complete'+id} onClick={()=>void complete(b)}>Complete production</Action></div>}{done&&<span className="status-pill">Completed · actual {n(b.actual_output_quantity)} {unit(s(b.output_unit_id))}</span>}</div>})}</div>
 <div className="console-panel"><div className="panel-heading"><div><h3>Step 4 · Dispatch</h3><span>Dispatch only stock that is ready.</span></div></div><div className="form-grid"><Field label="Dispatch date"><input type="date" value={dispatchDate||today()} onChange={e=>setDispatchDate(e.target.value)}/></Field><Field label="Dispatch method"><select value={dispatchMethod} onChange={e=>setDispatchMethod(e.target.value)}><option value="customer_pickup">Customer pickup</option><option value="own_delivery">Own delivery</option><option value="courier">Courier</option><option value="transport">Transport</option><option value="other">Other</option></select></Field></div><div className="table-wrap"><table><thead><tr><th>Product</th><th>Ordered</th><th>Already dispatched</th><th>Dispatch now</th></tr></thead><tbody>{lines.map(l=>{const remaining=Math.max(0,n(l.ordered_quantity)-n(l.dispatched_quantity));return <tr key={s(l.id)}><td>{s(l.item?.business_code||l.item?.item_code)} — {s(l.item?.name)}</td><td>{n(l.ordered_quantity)} {unit(s(l.unit_id))}</td><td>{n(l.dispatched_quantity)} {unit(s(l.unit_id))}</td><td><input className="compact-number" type="number" min="0" max={remaining} step="0.001" value={s(dispatchQty[s(l.id)]||'')} placeholder={`up to ${remaining}`} onChange={e=>setDispatchQty(z=>({...z,[s(l.id)]:e.target.value}))}/></td></tr>})}</tbody></table></div><div className="workflow-actions"><Action primary disabled={busy==='dispatch'} onClick={()=>void dispatch()}>Dispatch selected quantities</Action></div></div>
 <Status message={msg} error={err}/><div style={{height:12}}/></div></div>}
 </section>
}
