'use client'
import {useEffect,useState} from 'react'
import {createClient} from '@/lib/supabase/client'
import DataTable from '@/components/data-table'

type R=Record<string,any>
const db=()=>createClient();const s=(v:any)=>String(v??'');const n=(v:any)=>Number(v??0)
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="field"><span>{label}</span>{children}</label>}
function Status({message,error=false}:{message:string;error?:boolean}){return message?<p className={`form-status${error?' form-status-error':''}`} role="alert">{message}</p>:null}
export default function ProductionConsoleFixed(){
 const[batches,setBatches]=useState<R[]>([]),[items,setItems]=useState<R[]>([]),[units,setUnits]=useState<R[]>([]),[learning,setLearning]=useState<Record<string,R>>({}),[open,setOpen]=useState(false),[batchId,setBatchId]=useState(''),[selected,setSelected]=useState<R|null>(null),[plan,setPlan]=useState<R[]>([]),[output,setOutput]=useState(''),[varianceMode,setVarianceMode]=useState<'one_time'|'permanent'>('one_time'),[message,setMessage]=useState(''),[error,setError]=useState(false),[refreshToken,setRefreshToken]=useState(0),[busy,setBusy]=useState(false)
 const load=async()=>{const sb=db();const[{data:b},{data:i},{data:u},{data:l}]=await Promise.all([sb.from('v_production_batches_list').select('*').order('production_date',{ascending:false}),sb.from('items').select('*').eq('is_active',true).order('name'),sb.from('units').select('*').eq('is_active',true).order('name'),sb.from('v_production_yield_learning').select('*')]);setBatches(b??[]);setItems(i??[]);setUnits(u??[]);const m:Record<string,R>={};for(const x of l??[])m[s(x.recipe_version_id)]=x;setLearning(m)}
 useEffect(()=>{void load()},[])
 const choose=async(id:string)=>{setBatchId(id);setMessage('');setError(false);const b=batches.find(x=>s(x.id)===id)??null;setSelected(b);setOutput('');setVarianceMode('one_time');if(!id){setPlan([]);return}const{data,error:e}=await db().from('production_batch_plan_lines').select('id,ingredient_item_id,planned_quantity,unit_id').eq('production_batch_id',id).order('id');if(e){setError(true);setMessage(e.message);setPlan([]);return}setPlan((data??[]).map(x=>({id:x.id,itemId:s(x.ingredient_item_id),unitId:s(x.unit_id),planned:n(x.planned_quantity),actual:''})))}
 const approve=async()=>{if(!batchId)return;setBusy(true);const{error:e}=await db().rpc('approve_production_batch',{p_production_batch_id:batchId});setBusy(false);if(e){setError(true);setMessage(e.message);return}setError(false);setMessage('Production plan approved by wife.');await load();setSelected(p=>p?{...p,wife_approval_status:'approved'}:p);setRefreshToken(v=>v+1)}
 const start=async()=>{if(!batchId)return;setBusy(true);const{error:e}=await db().rpc('start_production_batch',{p_production_batch_id:batchId});setBusy(false);if(e){setError(true);setMessage(e.message);return}setError(false);setMessage('Production started. Reserved ingredients are issued to this batch.');await load();setSelected(p=>p?{...p,status:'in_progress'}:p);setRefreshToken(v=>v+1)}
 const complete=async()=>{if(!selected)return;const consumptions=plan.filter(x=>n(x.actual)>0).map(x=>({ingredient_item_id:x.itemId,actual_quantity:n(x.actual),unit_id:x.unitId}));if(consumptions.length!==plan.length||n(output)<=0){setError(true);setMessage('Enter an actual quantity for every ingredient and the actual finished output.');return}const hasVariance=plan.some(x=>Math.abs(n(x.actual)-n(x.planned))>0.000001)||Math.abs(n(output)-n(selected.planned_output_quantity))>0.000001;setBusy(true);const{error:e}=await db().rpc('complete_production_with_learning',{p_production_batch_id:batchId,p_consumptions:consumptions,p_outputs:[{output_item_id:selected.output_item_id,quantity:n(output),unit_id:selected.output_unit_id}],p_variance_mode:hasVariance?varianceMode:'one_time'});setBusy(false);if(e){setError(true);setMessage(e.message);return}setError(false);setMessage(varianceMode==='permanent'&&hasVariance?'Production completed and a new recipe version was saved from the actual result.':'Production completed; actual consumption and output were recorded for future estimates.');await load();setRefreshToken(v=>v+1);setOpen(false)}
 const itemName=(id:string)=>s(items.find(i=>s(i.id)===id)?.name||id),itemCode=(id:string)=>s(items.find(i=>s(i.id)===id)?.item_code||items.find(i=>s(i.id)===id)?.business_code||''),unitName=(id:string)=>s(units.find(u=>s(u.id)===id)?.symbol||units.find(u=>s(u.id)===id)?.name||'')
 const learn=learning[s(selected?.recipe_version_id)],planned=n(selected?.planned_output_quantity),standard=n(learn?.recipe_expected_output||planned),learnedEstimate=standard>0?n(learn?.learned_expected_output||standard)*planned/standard:planned
 return <section className="page-panel"><div className="section-label">Mane Masala</div><div className="master-header"><div><h1>Production</h1><p className="page-intro">Review recipe and plan, approve, issue ingredients, then record actual consumption and actual finished output. Completed batches improve future yield estimates.</p></div><button className="primary-button" type="button" onClick={()=>{setOpen(true);setMessage('');setError(false)}}>+ Review Production</button></div>
 {open&&<div className="modal-backdrop"><div className="modal-card purchase-modal"><div className="modal-header"><h3>Production batch — review & completion</h3><button className="secondary-button" type="button" onClick={()=>setOpen(false)}>Close</button></div><Field label="Production batch"><select value={batchId} onChange={e=>choose(e.target.value)}><option value="">Select production batch...</option>{batches.filter(b=>s(b.status)!=='completed').map(b=><option key={s(b.id)} value={s(b.id)}>{s(b.business_code)} — {s(b.output_item_name)} — {s(b.status)} / {s(b.wife_approval_status)}</option>)}</select></Field>{selected&&<><div className="plan-summary"><div><small>Order</small><strong>{s(selected.order_code)}</strong></div><div><small>Recipe</small><strong>v{s(selected.recipe_version)}</strong></div><div><small>Production target</small><strong>{planned} {unitName(s(selected.output_unit_id))}</strong></div></div><div className="plan-summary"><div><small>Recipe standard</small><strong>{standard} {unitName(s(selected.output_unit_id))}</strong></div><div><small>Learned expected output</small><strong>{learnedEstimate.toFixed(3)} {unitName(s(selected.output_unit_id))}</strong></div><div><small>Learning sample</small><strong>{n(learn?.completed_batches||0)} batch{n(learn?.completed_batches)===1?'':'es'}</strong></div></div><div className="console-panel"><div className="panel-heading"><h3>Ingredients — planned vs actual</h3><span>Actual values are required for completion.</span></div><div className="table-wrap"><table className="production-plan-table"><thead><tr><th>Ingredient</th><th>Planned</th><th>Unit</th><th>Actual</th></tr></thead><tbody>{plan.map((p,i)=><tr key={s(p.id)}><td><strong>{itemCode(p.itemId)}</strong> — {itemName(p.itemId)}</td><td className="num">{p.planned}</td><td>{unitName(p.unitId)}</td><td><input aria-label={`Actual quantity for ${itemName(p.itemId)}`} type="number" min="0" step="0.001" value={p.actual} placeholder="0.000" onChange={e=>setPlan(v=>v.map((z,j)=>j===i?{...z,actual:e.target.value}:z))}/></td></tr>)}</tbody></table></div></div><div className="form-grid"><Field label={`Actual finished output (${unitName(s(selected.output_unit_id))})`}><input type="number" min="0" step="0.001" value={output} placeholder="0.000" onChange={e=>setOutput(e.target.value)}/></Field><Field label="Wife approval"><input value={s(selected.wife_approval_status)} readOnly/></Field></div>{plan.some(x=>Math.abs(n(x.actual)-n(x.planned))>0.000001)||Math.abs(n(output)-planned)>0.000001?<div className="console-panel"><div className="panel-heading"><h3>Production variance</h3><span>Choose whether this result is batch-specific or becomes the next recipe version.</span></div><Field label="Variance treatment"><select value={varianceMode} onChange={e=>setVarianceMode(e.target.value as 'one_time'|'permanent')}><option value="one_time">One time — this batch only</option><option value="permanent">Permanent — save new recipe version</option></select></Field></div>:null}<Status message={message} error={error}/><div className="workflow-actions">{selected.wife_approval_status!=='approved'&&<button className="secondary-button" type="button" disabled={busy} onClick={approve}>Wife approve production plan</button>}{selected.wife_approval_status==='approved'&&selected.status==='planned'&&<button className="secondary-button" type="button" disabled={busy} onClick={start}>Start / issue production</button>}{selected.status==='in_progress'&&<button className="primary-button" type="button" disabled={busy} onClick={complete}>{busy?'Saving…':'Complete production'}</button>}</div></>}</div></div>}
 <div className="console-panel"><div className="panel-heading"><h2>Production batches</h2></div><DataTable table="v_production_batches_list" refreshToken={refreshToken} linkColumns={['business_code']}/></div></section>
}
