# Mane Masala Documentation

The authoritative overall project state is **MM-BUSINESS-SPEC-1.6** in the Project Library. This documentation set supports it; it does not replace it.

- `master-state/` — state/version continuity
- `architecture/` — approved technical architecture
- `business-rules/` — durable operational rules
- `database/` — schema/integrity reference
- `workflows/` — end-to-end business flows
- `testing/` — validation and test matrix
- `decisions/` — approved/open decision register
- `implementation/` — implementation notes and source mapping
- `migration/` — data migration rules
- `ux/` — wife-friendly UI standards
- `handover/` — next-chat continuity and implementation gaps
- `CHANGE_LOG.md` — chronological project changes

Supporting documents must not silently contradict the current Master State. When conflict is found, record it in the decision/gap register rather than guessing.
